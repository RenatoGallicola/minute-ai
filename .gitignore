# Virtual environment
venv/

# Generated outputs
outputs/

# Audio files (often large)
inputs/

# Logs
logs/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.pytest_cache/

# Config with sensitive tokens — never commit!
config.py

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# pip-tools compiled file (optional: some teams commit this, some don't)
# Uncomment the line below if you prefer not to commit the pinned requirements
# requirements.txt

# Node tooling used only to (re)compile static/tailwind.css — never a runtime dependency
node_modules/
package.json
package-lock.json

# Claude Code project context — kept local only, not shared in the repo
CLAUDE.md
