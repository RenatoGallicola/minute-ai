# minute-ai: direct dependencies
# This file lists only the packages you explicitly depend on.
# Run `pip-compile requirements.in` to regenerate requirements.txt with pinned versions.
# Run `pip install -r requirements.txt` to install.

# Core
requests
psutil

# Transcription
whisperx

# Export formats
python-docx
reportlab

# GUI (FastAPI + Jinja2 + htmx; CSS is hand-written in static/app.css - no build step)
fastapi
uvicorn[standard]
python-multipart
jinja2

# Dev / testing
pytest

# PyTorch: install manually with the command below for your platform:
# pip install torch torchvision torchaudio
# See https://pytorch.org/get-started/locally/ for GPU/CUDA variants
